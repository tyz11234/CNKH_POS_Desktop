# CNKH POS Desktop 0.3.4+7

发布日期：2026-09-13。配套 Mobile 1.9.1+26，LAN 协议仍为 `cnkh-sync:v1`。

## 修复

- 修复单号并发重复、销售同步去重及小票改号后的库存流水关联。
- 修复同一商品多行进货撤销的数量计算，并加强流水缺失、数量不符和系统时间回拨时的撤销保护。
- 修复商品搜索对只读数据库结果排序导致的异常。
- 商品编辑按原始快照合并字段，保留期间更新的库存和成本；库存冲突时拒绝覆盖。
- 商品库存编辑记录流水，手机同步到电脑也保留流水，阻止不安全的旧进货撤销。
- 修复备份恢复成功后清理旧文件失败可能导致已恢复数据库被删除的问题。

## 下载与使用

下载 `CNKH_POS_Desktop-windows-x64-v0.3.4-7.zip`，完整解压后运行 `cnkh_pos_desktop.exe`；不要只复制 EXE，需保留同目录 DLL 和 data 文件夹。此包是 Windows x64 便携包，不是安装向导。

更新前在现有程序中备份业务数据并关闭程序，再将新版解压到单独目录。确认数据正常后再处理旧目录。

附件 `SHA256SUMS.txt` 提供安装包 SHA-256 校验值。

## 验证

- 发布流程在上传附件前执行静态分析、86 项 Flutter 测试及 Windows Release 构建。
- 本轮双端 6 项 HTTP 同步及重连回归已通过。
- 未执行实体设备、打印机及真实门店局域网验收。

[手机版 1.9.1](https://github.com/tyz11234/CNKH_POS_Mobile_APK/releases/tag/v1.9.1-mobile)
