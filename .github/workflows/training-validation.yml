name: Training screenshot validation
on:
  push:
    branches: [feature/myinvois-modular]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      - run: flutter test tool/training_capture_test.dart
      - run: flutter test tool/training_view_test.dart
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: training-screenshots-check
          path: assets/training/
      - uses: actions/upload-artifact@v4
        with:
          name: training-view-check
          path: build/training-preview/
      - uses: actions/upload-artifact@v4
        with:
          name: desktop-resolved-dependencies
          path: pubspec.lock
